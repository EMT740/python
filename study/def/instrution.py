def add(x, y):
    """
    add函数可以接收两个参数，进行2相加的功能
    :param x:形参x表示相加的一个数字
    :param y:形参y表示相加的一个数字
    :return:返回值是两数相加的结果
    """
    result = x + y
    print(f"两数相加的结果是:{result}")
    return result


add(1, 2)
